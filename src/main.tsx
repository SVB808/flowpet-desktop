import React from 'react';import ReactDOM from 'react-dom/client';import { getCurrentWindow } from '@tauri-apps/api/window';import App from './App';import PetApp from './PetApp';import './styles.css';async function boot(){let label='main';try{label=getCurrentWindow().label}catch{}ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode>{label==='pet'?<PetApp/>:<App/>}</React.StrictMode>)}void boot();
